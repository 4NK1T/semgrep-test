name: Security Scan

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  sast:
    uses: 4NK1T/security-workflows/.github/workflows/semgrep.yml@main
    secrets: inherit
